packages:
  - "shared"
  - "cabinet"
  - "admin"

# Moved here from package.json: pnpm 10 and later read settings from this file,
# and pnpm 11 warns about — and ignores — the "pnpm" field in package.json.
onlyBuiltDependencies:
  - "@biomejs/biome"
  - "esbuild"
