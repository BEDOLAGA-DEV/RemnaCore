package multisub

import "github.com/BEDOLAGA-DEV/RemnaCore/pkg/naming"

const (
	// PlatformTag identifies platform-managed users in the VPN panel.
	PlatformTag = naming.PlatformTag

	// DefaultTrafficStrategy is the default traffic accounting strategy
	// applied when creating VPN panel users.
	DefaultTrafficStrategy = "MONTH"
)
